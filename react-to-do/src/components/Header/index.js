import React from "react";

import classes from "./index.module.css";

const Header = () => (
  <div className={classes.Header}>
    <span className={classes.Title}>REACT TO-DO</span>
  </div>
);

export default Header;
